services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: FraudCheck-postgres
    environment:
      POSTGRES_DB: FraudCheck
      POSTGRES_USER: FraudCheck
      POSTGRES_PASSWORD: FraudCheck
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U FraudCheck -d FraudCheck"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: FraudCheck-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # LocalStack for local AWS services
  localstack:
    image: localstack/localstack:latest
    container_name: FraudCheck-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
      - DEBUG=0
      - DATA_DIR=/var/lib/localstack/data
      - PERSISTENCE=1
    volumes:
      - localstack_data:/var/lib/localstack
      - "/var/run/docker.sock:/var/run/docker.sock"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # ClamAV Daemon for malware scanning
  clamav:
    image: clamav/clamav:stable
    platform: linux/amd64
    container_name: FraudCheck-clamav
    mem_limit: 4g
    mem_reservation: 3g
    environment:
      - CLAMAV_NO_FRESHCLAMD=false
      - CLAMAV_NO_CLAMD=false
      - CLAMD_CONF_LogVerbose=yes
      - CLAMD_CONF_DatabaseDirectory=/var/lib/clamav
      - CLAMD_CONF_LocalSocket=/tmp/clamd.sock
      - CLAMD_CONF_LocalSocketGroup=clamav
      - CLAMD_CONF_LocalSocketMode=666
      - CLAMD_CONF_FixStaleSocket=yes
      - CLAMD_CONF_TCPSocket=3310
    volumes:
      - clamav_data:/var/lib/clamav
      - clamav_socket:/tmp
    healthcheck:
      test: ["CMD", "test", "-S", "/tmp/clamd.sock"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 3m
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: FraudCheck-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://FraudCheck:FraudCheck@postgres:5432/FraudCheck
      - AWS_ENDPOINT_URL=http://localstack:4566
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - INIT_DB=true
      - CLAMAV_SOCKET_PATH=/tmp/clamd.sock
    env_file:
      - .env.development
    volumes:
      - ./backend:/app
      - backend_uploads:/app/uploads
      - clamav_socket:/tmp
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
      clamav:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Celery Worker
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: FraudCheck-celery-worker
    command: python celery_worker.py
    environment:
      - DATABASE_URL=postgresql+asyncpg://FraudCheck:FraudCheck@postgres:5432/FraudCheck
      - AWS_ENDPOINT_URL=http://localstack:4566
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - CLAMAV_SOCKET_PATH=/tmp/clamd.sock
    env_file:
      - .env.development
    volumes:
      - ./backend:/app
      - backend_uploads:/app/uploads
      - clamav_socket:/tmp
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      localstack:
        condition: service_healthy
      clamav:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Flower Monitoring (Optional)
  flower:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: FraudCheck-flower
    # Celery CLI parses broker options as "celery [celery args] flower [flower args]".
    # Passing --broker after "flower" causes the warning seen in logs.
    command: celery -A app.tasks.celery_app --broker=redis://redis:6379/0 flower --port=5555 --basic_auth=admin:admin
    ports:
      - "5555:5555"
    environment:
      - DATABASE_URL=postgresql+asyncpg://FraudCheck:FraudCheck@postgres:5432/FraudCheck
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    env_file:
      - .env.development
    depends_on:
      - redis
      - celery-worker
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Frontend Application
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: FraudCheck-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.development
    environment:
      - API_BASE_URL=http://backend:8000
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
      - NODE_ENV=development
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - FraudCheck-network

  # Optional: pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: FraudCheck-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@FraudCheck.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "8080:80"
    depends_on:
      - postgres
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    restart: unless-stopped
    networks:
      - FraudCheck-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  pgadmin_data:
    driver: local
  localstack_data:
    driver: local
  backend_uploads:
    driver: local
  clamav_data:
    driver: local
  clamav_socket:
    driver: local

networks:
  FraudCheck-network:
    driver: bridge