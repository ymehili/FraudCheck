# Copy to .env.development for local dev (Docker Compose).
# Note: backend/app/core/config.py requires these to be set and not equal to: "", "test", "changeme", "default".

# AWS / LocalStack (LocalStack accepts arbitrary credentials)
AWS_ACCESS_KEY_ID=localstack
AWS_SECRET_ACCESS_KEY=localstack_secret
AWS_REGION=us-east-2
S3_BUCKET_NAME=FraudCheck-local-bucket

# Clerk (replace with your real Clerk keys for auth to work end-to-end)
CLERK_PUBLISHABLE_KEY=pk_test_replace_me
# Frontend expects this name (Next.js exposes NEXT_PUBLIC_* to the browser)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
CLERK_SECRET_KEY=sk_test_replace_me

# Gemini (replace with a real key for analysis features)
GEMINI_API_KEY=replace_me


